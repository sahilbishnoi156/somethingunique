import BottomNavigation from '@/components/navigation/bottom-navigation';

import type { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Something Unique - Login',
    description: 'Generated by create next app',
};

export default function RootLayout({
    children,
}: Readonly<{
    children: React.ReactNode;
}>) {
    return (
        <div className="p-3 h-screen w-screen flex items-center justify-center gap-10 bg-secondary ">
            <div className="h-full w-3/4 flex flex-col gap-5">
                <div className="h-[92%] rounded-xl">{children}</div>
                <div className="h-[8%] rounded-xl">
                    <BottomNavigation />
                </div>
            </div>
            <div className="bg-foreground flex-1 h-full rounded-xl"></div>
        </div>
    );
}
